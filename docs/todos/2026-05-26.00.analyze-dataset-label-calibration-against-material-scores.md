# TODO: Analyze dataset label calibration against material scores

## Current Status

- State: Not Yet Started
- Dropped Reason: 
- Last Status Update: 2026-05-26 15:09:12

## Context

The parallel CNN attention fusion experiment uses a fixed material-prior logit plus a positional residual head. Training showed weaker-than-expected early dynamics and unusually heavy grad-norm tails. Full evaluation completed with test BCE 0.6040597092544988 and ECE 0.0026435719234850644, which failed to beat the stacked edge-gate FFN reference. The user suspects the dataset labels may not follow the expected material-score distribution closely enough for the fixed prior to be harmless. This follow-up is now unblocked and queued for the next analysis pass.

## Why It Is Needed

If empirical labels are poorly calibrated against raw material difference, fixed material-prior scaling may inject systematic residual burden and optimization spikes. The analysis should quantify whether labels follow material score as expected, where they diverge, and whether divergence explains the current experiment behavior.

## Completion Criteria

Produce a report comparing dataset labels against raw material difference and material-prior logits across train/validation/test splits. Include binned label rates, sample counts, calibration curves, residual distributions, outlier examples or aggregate buckets, and implications for material-prior model design. Record conclusions in docs/experiments or docs/designs as appropriate.

## Detailed Plan

1. Reuse the processed dataset readers and raw board material-diff helper. 2. Sample or stream train, validation, and test splits with deterministic limits. 3. Compute raw material diff, material-prior probability/logit, label mean, BCE, calibration error, and residual label-minus-prior summaries by material bucket. 4. Break down by absolute material imbalance, side-to-move orientation, and extreme material buckets if available from the encoded board. 5. Save compact reports under artifacts/reports and summarize findings in a docs experiment log. 6. Use the result to decide whether fixed material prior should be rescaled, learned, softened, or removed.

## Pseudo Code Diff

```diff
+ python/analyze_material_label_calibration.py
+   def run_analysis(split: str, rows: int | None, seed: int, output: Path) -> dict:
+       dataset = ChessEvaluationDataset(split_path)
+       for board_tensor, label in deterministic_batches(dataset):
+           material_diff = _material_diff_from_board(board_tensor, _MATERIAL_VALUES)
+           prior_logit = material_diff * (log(10) / 4)
+           collect_bucket_stats(material_diff, prior_logit, label)
+       return write_json_report(stats)
+
+ python/cli.py
+   app.command("analyze-material-labels")(run_analysis_cli)
+
+ docs/experiments/YYYY-MM-DD.NN-material-label-calibration.md
+   Record setup, sampled rows, reports, tables, and conclusions.
```


## Activity Log

- [2026-05-26 14:51:05] [USER] User requested that, after the current experiment finishes, the dataset should be analyzed to compare labels against material scores because the fixed prior may not match the empirical label distribution.
- [2026-05-26 14:51:05] [ACTION] Created a tracked TODO for material-label calibration analysis after the current full experiment.
- [2026-05-26 14:51:05] [RESULT] TODO records scope, rationale, completion criteria, analysis plan, and a pseudo code diff for the follow-up.
- [2026-05-26 15:09:12] [USER] User requested dataset analysis after the current experiment because labels may diverge from expected material-score behavior.
- [2026-05-26 15:09:12] [ACTION] Updated the TODO after the parallel fusion full evaluation completed.
- [2026-05-26 15:09:12] [RESULT] TODO is unblocked but remains not started; it records the final experiment context and target analysis.
