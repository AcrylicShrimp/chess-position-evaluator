# TODO: Retrain stacked edge-gate FFN on cleaned data

## Current Status

- State: Partially Taken
- Dropped Reason:
- Last Status Update: 2026-05-26 20:48:12

## Context

The current parallel-fusion material-features run is still in progress. The source/staging cleanup work found that material-derived diagnostics were previously misleading, and the dataset has since been regenerated with stricter filtering. The strongest reference model so far is the stacked edge-gate FFN warmup-cosine run, but it was trained before the cleaned-data cycle was fully established.

## Why It Is Needed

A clean-data retrain is needed to confirm whether the stacked edge-gate FFN result remains the best architecture under the revised dataset. This also provides a fairer control for judging the current parallel-fusion material-features experiment and future architecture changes.

## Completion Criteria

- Cleaned processed train/validation/test datasets are confirmed available.
- The stacked edge-gate FFN warmup-cosine recipe is reconstructed from the prior successful experiment log.
- A new full training run completes with a distinct experiment name.
- Full validation and test evaluation reports are generated.
- The new result is compared against the historical stacked edge-gate FFN result and the current parallel-fusion material-features result.
- An experiment log records setup, artifacts, metrics, interpretation, and pass/fail status when meaningful.

## Detailed Plan

1. Wait for the current parallel-fusion material-features run and evaluation to complete.
2. Confirm the cleaned data artifact paths and row counts.
3. Recreate the previous stacked edge-gate FFN warmup-cosine recipe without changing unrelated architecture or training knobs.
4. Start a new full training run with a name that clearly marks the cleaned-data retrain.
5. Track 10-epoch checkpoints and compare the trajectory against prior logs.
6. Run full validation and test evaluation on the best checkpoint.
7. Record conclusions in docs/experiments and decide whether the cleaned-data stacked model becomes the new primary baseline.

## Pseudo Code Diff

Not applicable.

## Activity Log

- [2026-05-26 19:05:55] [USER] User requested a TODO to retrain stacked edge-gate FFN on the cleaned data after the current parallel-fusion experiment finishes.
- [2026-05-26 19:05:55] [ACTION] Created a tracked follow-up TODO for the cleaned-data stacked edge-gate FFN retraining experiment.
- [2026-05-26 19:05:55] [RESULT] Follow-up is documented and blocked until the current parallel-fusion run and evaluation finish.
- [2026-05-26 20:48:12] [USER] User asked to commit the completed material-feature work and start the cleaned-data stacked edge-gate FFN retraining experiment.
- [2026-05-26 20:48:12] [ACTION] Marked the retraining TODO as in progress after committing the previous material-signal and parallel-fusion work.
- [2026-05-26 20:48:12] [RESULT] The cleaned-data stacked edge-gate FFN retraining task is now active.
