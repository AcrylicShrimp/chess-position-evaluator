# TODO: Refactor Model Blocks Into Reusable Modules

## Current Status

- State: Finished
- Dropped Reason:
- Last Status Update: 2026-05-28 23:06:03

## Context

python/libs/model.py contains model constants, material helpers, coordinate/ghost primitives, attention blocks, trunk assemblies, value heads, policy head, variant dispatch, and model factories in one large module. New experiments keep adding variants that reuse many of the same blocks.

## Why It Is Needed

The current single-file model surface makes it hard to add or compare variants without duplicating logic or touching unrelated behavior. Separating reusable primitives from variant assembly should reduce experiment risk while preserving checkpoint behavior.

## Completion Criteria

Model implementation is split into reusable modules with stable public imports, existing checkpoints still load, all model tests pass, parameter counts for existing variants remain unchanged, and no training/evaluation behavior changes are introduced intentionally.

## Detailed Plan

1. Introduce an internal python/libs/modeling/ package for model implementation pieces.
2. Move material helpers, primitive blocks, attention blocks, trunks, and heads into separate modules.
3. Keep python/libs/model.py as the stable compatibility facade exporting the existing public names.
4. Preserve existing ValueOnlyModel and ModelFull constructors and checkpoint state dict keys.
5. Run focused model tests and full unit tests after each slice.

## Pseudo Code Diff

```diff
+ python/libs/modeling/material.py
+   _MATERIAL_VALUES
+   material_diff_from_board(...)
+   material_feature(...)
+   material_input_features(...)
+
+ python/libs/modeling/primitives.py
+   AddCoords
+   CoordinateAttention
+   GhostModule
+   ChannelShuffle
+   GhostShuffleBlock
+   ChannelProjection
+
+ python/libs/modeling/attention.py
+   BOARD_ATTENTION_* constants
+   NaiveBoardSelfAttention
+   BoardAttentionFFN
+   BoardAttentionBlock
+   BoardAttentionStack
+   IdentityBoardAttention
+
+ python/libs/modeling/trunks.py
+   ParallelCnnAttentionTrunk
+   FunnelCnnAttentionTrunk
+   FunnelInterleavedAttentionTrunk
+   FunnelDepthRefreshAttentionTrunk
+   ParallelCnnAttentionAlignedAddTrunk
+   ParallelCnnAttentionKEdgeLateEvidenceTrunk
+   AttentionCnnRefreshBlock
+   AttentionPairCnnRefreshBlock
+   _run_trunk_blocks(...)
+
+ python/libs/modeling/heads.py
+   ValueHead
+   ResidualValueHead
+   MaterialFeatureValueHead
+   LateEvidenceValueHead
+   PolicyHead
+
+ python/libs/modeling/factory.py
+   ValueOnlyModel
+   ModelFull
+   build_board_attention(...)
+
~ python/libs/model.py
+   # compatibility facade: re-export existing public API from libs.modeling
```

## Activity Log

- [2026-05-28 22:46:33] [USER] Requested TODO tracking for model variant and shared block refactoring.
- [2026-05-28 22:46:33] [ACTION] Created TODO for splitting reusable model blocks from variant assembly.
- [2026-05-28 22:46:33] [RESULT] Model modularization workstream is tracked as a behavior-preserving code refactor.
- [2026-05-28 22:51:44] [ACTION] Incorporated fresh-context review feedback by expanding the pseudo diff to name omitted current trunk/helper blocks.
- [2026-05-28 22:51:44] [RESULT] Model modularization TODO now covers aligned-add, k-edge late-evidence, refresh helper blocks, and shared trunk execution helper boundaries.
- [2026-05-28 23:01:19] [USER] Requested continuing the TODO workstreams one by one with a commit after each completed item.
- [2026-05-28 23:01:19] [ACTION] Started the model modularization workstream by inspecting the current model.py boundaries and public imports.
- [2026-05-28 23:01:19] [RESULT] Next implementation slice is a behavior-preserving split into libs.modeling modules with libs.model kept as the compatibility facade.
- [2026-05-28 23:06:03] [ACTION] Split python/libs/model.py into libs.modeling constants, material, primitives, attention, trunks, heads, and factory modules while keeping libs.model as the compatibility facade.
- [2026-05-28 23:06:03] [RESULT] Model public imports, state_dict keys, parameter counts, and seeded forward outputs were preserved for supported variants and ModelFull; focused and full unit tests passed.
