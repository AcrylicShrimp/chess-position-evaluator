# TODO: Add checkpoint lifecycle tests after model bake cycle

## Current Status

- State: Not Yet Started
- Dropped Reason:
- Last Status Update: 2026-05-29 00:02:30

## Context

The initial cleanup tracker intentionally deferred checkpoint lifecycle compatibility tests because stale experiment checkpoints were allowed to break while the model architecture and export flow were still changing. The project now tracks runtime artifacts under artifacts/ and has a central model variant registry, but a stable bake/export cycle has not yet been declared.

## Why It Is Needed

Once the model bake/export loop stabilizes, tests should verify that newly produced checkpoints can be loaded, resumed, evaluated, and exported without relying on stale default names or accidental compatibility behavior.

## Completion Criteria

A focused test or smoke workflow covers a freshly baked checkpoint lifecycle: train or create a minimal checkpoint fixture, resolve its model_variant metadata, load it into ValueOnlyModel, resume from it when appropriate, run eval-dataset or an equivalent smoke path, and export ONNX shape metadata. The test must avoid hardcoding volatile experiment checkpoint names.

## Detailed Plan

1. Wait until a current model bake/export cycle is considered stable enough to preserve.
2. Define a tiny deterministic checkpoint fixture or short smoke-training command that is cheap for CI/local verification.
3. Add tests for checkpoint metadata resolution, strict state_dict load, resume construction order, eval-dataset smoke behavior, and ONNX export input/output shape.
4. Keep old experiment checkpoints outside the compatibility promise unless explicitly promoted.
5. Update README, command catalog, or runbooks if the lifecycle contract becomes a documented workflow.

## Pseudo Code Diff

```diff
+ python/tests/test_checkpoint_lifecycle.py
+   def test_checkpoint_fixture_loads_with_recorded_variant(): ...
+   def test_resume_constructs_model_from_checkpoint_variant(): ...
+   def test_exported_onnx_shape_matches_encoder_contract(): ...
+
~ python/tests/fixtures/
+   # Optional tiny generated checkpoint fixture, only if cheap and deterministic.
+
~ docs/runbooks/full-training-experiment.md
+   # Document the promoted checkpoint lifecycle verification command when finalized.
```

## Activity Log

- [2026-05-29 00:02:30] [USER] Requested cleanup closure and asked to identify decisions only if needed.
- [2026-05-29 00:02:30] [ACTION] Split deferred checkpoint lifecycle testing out of the umbrella cleanup TODO.
- [2026-05-29 00:02:30] [RESULT] Checkpoint lifecycle testing has a dedicated follow-up tracker and no longer blocks closing the original cleanup umbrella.
