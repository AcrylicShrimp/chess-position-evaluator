# TODO: Design principled local global branch fusion projection

## Current Status

- State: Finished
- Dropped Reason:
- Last Status Update: 2026-05-27 12:53:18

## Context

The parallel fusion diagnostic showed local and global branch representations are nearly orthogonal by flattened cosine similarity and have different activation scales. The current 1x1 fusion concatenates branches without an explicit contract that they live in compatible feature spaces.

## Why It Is Needed

The user wants a more rigorous approach than naive concatenation: analyze at the formula level why the CNN local branch and attention/global branch differ, then design how to map them into a shared representation space before fusion.

## Completion Criteria

A design document derives the branch representation mismatch, defines a shared-space projection/fusion contract with equations, lists invariants and failure modes, and proposes the next implementation slice with verification criteria.

## Detailed Plan

1. Read the exact local branch, attention branch, FFN, normalization, and fuse definitions.
2. Write the current forward equations for shared, local, global, concat, fuse, and value head paths.
3. Analyze scale, basis, locality/globality, residual, normalization, and covariance mismatch risks.
4. Compare candidate mappings: pre-fusion normalization, per-branch projection, residual aligned fusion, gated fusion, and cross-branch conditioning.
5. Select a minimal controlled design and record equations, invariants, and tests.
6. Convert the selected design into an implementation TODO or experiment log before coding.

## Pseudo Code Diff

```diff
+ docs/designs/YYYY-MM-DD.NN.principled-parallel-branch-fusion.md
+ # Design: Principled Parallel Branch Fusion
+
+ current equations:
+   s = shared(x)
+   l = local_cnn(s)
+   g = global_attention(s)
+   y = head(fuse(concat(l, g)), optional_features)
+
+ proposed contract shape:
+   l_hat = P_l(N_l(l))
+   g_hat = P_g(N_g(g))
+   z = F(l_hat, g_hat, optional_gate(l_hat, g_hat))
+   y = head(z)
+
+ invariants:
+   shape(l_hat) == shape(g_hat) == [B, C, 8, 8]
+   scale(l_hat) and scale(g_hat) are bounded and comparable
+   fusion output preserves spatial board indexing
```

## Activity Log

- [2026-05-27 11:56:32] [USER] Requested tracking all three follow-ups and emphasized that item 2 needs formula-level analysis of why branch spaces differ and how to map them into a common space.
- [2026-05-27 11:56:32] [ACTION] Created TODO for a design-first branch-fusion projection analysis.
- [2026-05-27 11:56:32] [RESULT] TODO initialized for principled local/global fusion design.
- [2026-05-27 12:28:52] [USER] Asked to start the formula-level analysis and redesign briefing for the principled local/global branch fusion action item.
- [2026-05-27 12:28:52] [ACTION] Moved the branch-fusion design TODO to in-progress and began design-only current-state analysis.
- [2026-05-27 12:28:52] [RESULT] Design work started without implementation changes.
- [2026-05-27 12:28:52] [ACTION] Read the current model definitions and previous parallel-fusion design/diagnostic documents for formula-level current-state analysis.
- [2026-05-27 12:28:52] [RESULT] Current fusion is equivalent to a static per-square mixture z = HSwish(BN(W_l L + W_g G)), with no explicit branch-space alignment before mixing.
- [2026-05-27 12:49:43] [USER] Selected option 1, aligned add fusion, for the first branch-fusion test.
- [2026-05-27 12:49:43] [ACTION] Created the finalized design document for principled parallel branch fusion with aligned add as the first implementation slice.
- [2026-05-27 12:49:43] [RESULT] Design consensus recorded at docs/designs/2026-05-27.00-principled-parallel-branch-fusion.md.
- [2026-05-27 12:53:18] [ACTION] Implemented the aligned-add fusion variant and recorded a CUDA smoke experiment for the first selected implementation slice.
- [2026-05-27 12:53:18] [RESULT] Design TODO completed with finalized design doc docs/designs/2026-05-27.00-principled-parallel-branch-fusion.md and smoke log docs/experiments/2026-05-27.01-parallel-cnn-attn-aligned-add-smoke.md.
