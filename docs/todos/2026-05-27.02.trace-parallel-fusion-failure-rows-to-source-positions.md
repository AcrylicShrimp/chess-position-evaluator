# TODO: Trace parallel fusion failure rows to source positions

## Current Status

- State: Finished
- Dropped Reason:
- Last Status Update: 2026-05-27 12:04:50

## Context

The parallel-fusion diagnostic identifies processed dataset indices with large loss deltas, but the current report does not show source FENs or source engine cp values. The cheapest next diagnostic is to map those indices back to actual positions if the processed dataset or staging pipeline preserves enough order information.

## Why It Is Needed

Top problematic rows suggest the parallel model may over-rely on material direction when the label contradicts material balance. Inspecting actual positions is needed to distinguish tactical exceptions, data noise, preprocessing artifacts, and genuine architecture weakness.

## Completion Criteria

The top loss-delta processed row indices are mapped to source FEN/cp records when possible, or the mapping limitation is documented with exact evidence. The diagnostic findings are appended to the experiment log.

## Detailed Plan

1. Inspect processed dataset format and preprocessing code to determine whether row order maps to staging/source rows.
2. Inspect staging/source artifacts without deleting or modifying source data.
3. Extract the top problematic dataset indices from the parallel diagnostic JSON.
4. Query or decode source records for those indices if possible.
5. Summarize positions, material diff, label, baseline prediction, parallel prediction, and likely reason for conflict.
6. Update the parallel-fusion diagnostics experiment log.

Completed with `cpe trace-processed-rows`. The trace report mapped the top 20 worst and top 5 best diagnostic examples back to staging FEN/cp rows:

- `artifacts/reports/ghost-ca-r4-256ch-parallel-cnn3-attn3ffn64-fuse1x1-materialfeatures-warmupcosine5-lr5e4-100e-best.parallel-diagnostics.validation.200000.source-trace.validation.worst20.best5.json`

The validated mapping required `python-chess` `STATUS_VALID` plus the repository strict preprocessing filter. The final trace matched diagnostic labels and material differences with max label probability delta `3.6770835487587306e-08` and max material delta `0.0`.

## Pseudo Code Diff

```diff
+ python/trace_processed_rows.py
+ def run_trace_processed_rows(report_path, split, indices, output_path):
+     mapping = resolve_processed_to_source_mapping(split)
+     rows = lookup_rows(mapping, indices)
+     write_json({"indices": indices, "rows": rows, "mapping_contract": mapping.contract})
+
~ python/cli.py
+ cpe trace-processed-rows --report PATH --split validation --top-worst N

~ docs/experiments/2026-05-27.00-parallel-fusion-branch-diagnostics.md
+ record source-position trace results or mapping limitation
```

## Activity Log

- [2026-05-27 11:56:32] [USER] Requested tracking all three follow-ups and starting with item 3 because tracing top failure rows is the lowest-cost next diagnostic.
- [2026-05-27 11:56:32] [ACTION] Created TODO for processed-row to source-position tracing.
- [2026-05-27 11:56:32] [RESULT] TODO initialized for immediate row-trace investigation.
- [2026-05-27 11:56:32] [ACTION] Started investigating whether processed dataset rows can be mapped back to source FEN and cp records.
- [2026-05-27 11:56:32] [RESULT] Work moved to in-progress state.
- [2026-05-27 12:04:50] [ACTION] Implemented and ran a validated source trace command for the top parallel-fusion diagnostic rows.
- [2026-05-27 12:04:50] [RESULT] Traced top `20` worst and top `5` best diagnostic rows back to FEN/cp records and updated the experiment log with the refined diagnosis.
