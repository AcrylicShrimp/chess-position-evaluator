# Experiment: ghost-ca-r4-funnel-cnn224-160-128-attn6-refresh3-edgegate-gate40-warmupcosine5-lr1e3

## Status

- State: Planned
- Verdict: Not Evaluated
- Started At: Unknown
- Completed At: Unknown
- Last Updated: 2026-05-28 17:00:05

## Goal

Run a 40-epoch gate for the depth-preserving refresh architecture with double the original base learning rate.

This experiment tests whether the `funnel-cnn224-160-128-attn6-refresh3-edgegate` model is learning-rate limited upward. The `lr=0.00025` run underfit badly under the same 100-epoch budget, so this run checks the opposite direction from the `lr=0.0005` reference.

## Hypothesis Or Expectation

Increasing the base learning rate to `0.001` may accelerate fitting and improve the early validation curve if the model is under-optimized at lower rates.

Expected positive evidence:

- sampled validation at epoch 40 at least competitive with the `lr=0.0005` gate value `0.6062`
- no NaN, divergence, or sustained validation degradation after warmup
- no severe grad-norm instability beyond what grad clipping can contain

## Key Characteristics

- Model variant: `funnel-cnn224-160-128-attn6-refresh3-edgegate`.
- Architecture is unchanged from the depth-refresh experiments.
- Training recipe is unchanged except `lr=0.001`.
- `eta_min` is intentionally kept at `0.000001` to isolate the base-LR change.
- This is a 40-epoch gate run, not a full promotion run.
- If the gate is promising, the follow-up is a 100-epoch full run with the same LR.

## Setup

| Field | Value |
| --- | --- |
| Branch | `main` |
| Source commit | `82dd403` |
| Runtime | `uv run` |
| Torch | `2.9.1+cu128` |
| Device | CUDA |
| Model variant | `funnel-cnn224-160-128-attn6-refresh3-edgegate` |
| Parameter count | `490877` |
| Train data | `data/processed/train.chesseval` |
| Validation data | `data/processed/validation.chesseval` |
| Train rows | `139910260` |
| Validation rows | `7772768` |
| Training run | `ghost-ca-r4-funnel-cnn224-160-128-attn6-refresh3-edgegate-gate40-warmupcosine5-lr1e3` |
| Final checkpoint | `artifacts/checkpoints/ghost-ca-r4-funnel-cnn224-160-128-attn6-refresh3-edgegate-gate40-warmupcosine5-lr1e3.pth` |
| Best checkpoint | `artifacts/checkpoints/ghost-ca-r4-funnel-cnn224-160-128-attn6-refresh3-edgegate-gate40-warmupcosine5-lr1e3-best.pth` |
| Training log | `artifacts/reports/ghost-ca-r4-funnel-cnn224-160-128-attn6-refresh3-edgegate-gate40-warmupcosine5-lr1e3.train.log` |

Training configuration:

| Field | Value |
| --- | --- |
| epochs | `40` |
| steps per epoch | `1024` |
| batch | `2048` |
| lr | `0.001` |
| wd | `0.0001` |
| scheduler | `warmup-cosine` |
| warmup epochs | `5` |
| warmup start factor | `0.1` |
| eta min | `0.000001` |
| grad clip | `5` |
| compile mode | `max-autotune` |
| train workers | `4` |
| validation workers | `2` |

Primary references:

| Reference | Sampled Val Epoch 40 | Full Validation BCE | Full Test BCE |
| --- | ---: | ---: | ---: |
| depth-refresh `lr=0.00025` | `0.6107` | `0.605336` | skipped |
| depth-refresh `lr=0.0005` gate40 | `0.6062` | not run | not run |
| depth-refresh `lr=0.0005` full100 | `0.6080` | `0.6031740756086265` | `0.6032937549366552` |

## Procedure

Planned training:

```bash
WANDB_MODE=online WANDB_SILENT=true PYTHONUNBUFFERED=1 uv run cpe train ghost-ca-r4-funnel-cnn224-160-128-attn6-refresh3-edgegate-gate40-warmupcosine5-lr1e3 \
  --model-variant funnel-cnn224-160-128-attn6-refresh3-edgegate \
  --epochs 40 \
  --steps 1024 \
  --batch 2048 \
  --lr 0.001 \
  --wd 0.0001 \
  --scheduler warmup-cosine \
  --warmup-epochs 5 \
  --warmup-start-factor 0.1 \
  --eta-min 0.000001 \
  --grad-clip 5 \
  --compile-mode max-autotune \
  --train-workers 4 \
  --val-workers 2
```

Post-gate checks:

```bash
PYTHONUNBUFFERED=1 uv run cpe eval-dataset ghost-ca-r4-funnel-cnn224-160-128-attn6-refresh3-edgegate-gate40-warmupcosine5-lr1e3-best --split validation --full --batch 4096 --device auto
```

## Results

- Training: Pending.
- Full validation evaluation: Pending.
- Grad-norm review: Pending.

## Verdict Rationale

Not evaluated. The gate run has not started yet.

## Issues And Anomalies

None observed yet.

## Follow-Up

- If sampled validation at epoch 40 is clearly better than `0.6062` and grad norms are acceptable, run a 100-epoch promotion experiment.
- If the run diverges or shows severe grad-norm spikes, keep `lr=0.0005` as the practical upper bound and investigate stability separately.

## Activity Log

- [2026-05-28 17:00:05] [USER] Proposed testing double the original learning rate after the half-LR run underfit.
- [2026-05-28 17:00:05] [ACTION] Created this 40-epoch gate experiment log for `lr=0.001`.
- [2026-05-28 17:00:05] [RESULT] The high-LR gate run is ready to start.
